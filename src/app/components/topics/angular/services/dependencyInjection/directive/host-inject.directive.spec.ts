import { HostInjectDirective } from './host-inject.directive';

describe('HostInjectDirective', () => {
  it('should create an instance', () => {
    const directive = new HostInjectDirective();
    expect(directive).toBeTruthy();
  });
});
