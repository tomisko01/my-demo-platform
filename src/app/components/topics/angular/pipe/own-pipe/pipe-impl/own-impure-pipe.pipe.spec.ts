import { OwnImpurePipePipe } from './own-impure-pipe.pipe';

describe('OwnImpurePipePipe', () => {
  it('create an instance', () => {
    const pipe = new OwnImpurePipePipe();
    expect(pipe).toBeTruthy();
  });
});
